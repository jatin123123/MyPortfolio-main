/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com", "aceternity.com"], // Combine both domains in a single array
  },
};

export default nextConfig;
